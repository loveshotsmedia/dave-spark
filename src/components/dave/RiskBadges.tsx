// RiskBadges component - disabled since alien/risks endpoint is not available in Dave 2.0
// This component previously showed risk alerts from the legacy API

export function RiskBadges() {
  // The alien/risks endpoint doesn't exist in Dave 2.0 API
  // Return null to hide the component until a replacement API is available
  return null;
}
